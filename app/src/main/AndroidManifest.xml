<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.einfohukum">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/bg1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/bg1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".fahmi.view.GetPeraturanActivity"></activity>
        <activity android:name=".fahmi.view.GetNaskahKermaActivity" />
        <activity android:name=".fahmi.view.GetPidanaActivity" />
        <activity android:name=".fahmi.view.GetDisiplinActivity" />
        <activity android:name=".fahmi.view.GetKkeActivity" />
        <activity android:name=".fahmi.view.TampilActivity" />
        <activity android:name=".fahmi.view.GetPshActivity" />
        <activity
            android:name=".login.SplashScreenAct"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".file.SuccessForm" />
    </application>

</manifest>